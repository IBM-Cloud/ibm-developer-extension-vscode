sudo: false

services:
  - docker

language: node_js

node_js: node

os:
  - linux

script:
  npm run lint

deploy:
  provider: script
  script: "npm run vscode:publish"
  skip_cleanup: true
  on:
    tags: true
